"""CLI tool — interact with the memory emulator directly.

Usage:
    python src/cli/memory_cli.py put "some text" --tags tag1,tag2
    python src/cli/memory_cli.py search "query"
    python src/cli/memory_cli.py latest --n 10
"""
